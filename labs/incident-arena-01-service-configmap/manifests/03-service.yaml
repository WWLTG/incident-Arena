apiVersion: v1
kind: Service
metadata:
  name: arena-web-service
  namespace: incident-arena-01
spec:
  selector:
    app: web
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
