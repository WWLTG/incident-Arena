apiVersion: v1
kind: Pod
metadata:
  name: arena-client
  namespace: incident-arena-01
  labels:
    app: arena-client
spec:
  containers:
    - name: client
      image: busybox:1.36
      command:
        - sh
        - -c
        - sleep 3600
      resources:
        requests:
          cpu: 5m
          memory: 8Mi
        limits:
          cpu: 50m
          memory: 32Mi
  restartPolicy: Always
