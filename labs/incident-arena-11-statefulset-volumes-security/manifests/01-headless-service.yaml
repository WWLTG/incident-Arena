apiVersion: v1
kind: Service
metadata:
  name: arena-db-headless
  namespace: incident-arena-11
spec:
  clusterIP: None
  selector:
    app: arena-db
  ports:
    - name: http
      port: 80
      targetPort: 80
