apiVersion: v1
kind: Pod
metadata:
  name: arena-client
  namespace: incident-arena-08
  labels:
    app: arena-client
spec:
  containers:
    - name: client
      image: curlimages/curl:8.10.1
      command:
        - sh
        - -c
        - sleep 36000
