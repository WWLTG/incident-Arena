apiVersion: v1
kind: Pod
metadata:
  name: arena-client
  namespace: incident-arena-06
  labels:
    app: arena-client
spec:
  restartPolicy: Never
  containers:
    - name: client
      image: busybox:1.36.1
      imagePullPolicy: IfNotPresent
      command:
        - sh
        - -c
        - sleep 36000
