apiVersion: v1
kind: ResourceQuota
metadata:
  name: arena-quota
  namespace: incident-arena-10
spec:
  hard:
    requests.cpu: "500m"
    requests.memory: 256Mi
    limits.cpu: "1"
    limits.memory: 512Mi
    pods: "5"
