apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: arena-report-reader
  namespace: incident-arena-05
rules:
  - apiGroups:
      - ""
    resources:
      - secrets
    verbs:
      - get
