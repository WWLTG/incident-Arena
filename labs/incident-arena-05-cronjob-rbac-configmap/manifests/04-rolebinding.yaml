apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: arena-report-reader-binding
  namespace: incident-arena-05
subjects:
  - kind: ServiceAccount
    name: arena-reporter
    namespace: incident-arena-05
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: arena-report-reader
